#pragma once

static const unsigned int ScreenWidth = 1000;
static const unsigned int ScreenHeight = 1000;
static const unsigned int UnitSize = 10;
static const float NumXUnits = ScreenWidth / UnitSize;
static const float NumYUnits = ScreenHeight / UnitSize;
static const float HalfNumXUnits = NumXUnits * 0.5f;
static const float HalfNumYUnits = NumYUnits * 0.5f;